name: Build Admin Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - uses: android-actions/setup-android@v4

      - name: Install deps
        working-directory: south-admin
        run: npm install --legacy-peer-deps

      - name: Build web
        working-directory: south-admin
        run: npm run build

      - name: Sync Capacitor
        working-directory: south-admin
        run: npx cap sync android

      - name: Build APK
        working-directory: south-admin/android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon
          mkdir -p ../outputs
          cp app/build/outputs/apk/debug/app-debug.apk ../outputs/south-wallet-admin.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: south-wallet-admin-apk
          path: south-admin/outputs/south-wallet-admin.apk
          retention-days: 90
